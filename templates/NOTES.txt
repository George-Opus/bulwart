Bulwark Webmail deployed ({{ .Values.configMode }} mode).

{{- if eq .Values.configMode "env" }}
JMAP server : {{ .Values.config.jmapServerUrl }}
The setup wizard is disabled (JMAP_SERVER_URL is set).
{{- else }}
Wizard mode: open the app and complete the first-launch setup wizard.
Config is persisted in the "config" PVC (/app/data/admin).
{{- end }}

{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
URL: https://{{ .host }}
{{- end }}
{{- else }}
No ingress. Port-forward to test:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "bulwark.fullname" . }} 3000:{{ .Values.service.port }}
{{- end }}

{{- if not (or .Values.sessionSecret.existingSecret .Values.sessionSecret.value) }}

WARNING: no SESSION_SECRET configured. Set sessionSecret.existingSecret
(recommended) or sessionSecret.value.
{{- end }}
